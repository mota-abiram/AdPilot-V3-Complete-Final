services:
  db:
    image: postgres:latest
    container_name: adpilot-db
    environment:
      POSTGRES_DB: ad-pilot
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - adpilot-data:/var/lib/postgresql/data

volumes:
  adpilot-data:
